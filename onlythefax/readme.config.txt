#OCR
sudo pip install pytesseract   

sudo apt-get install tesseract-ocr
# pulled in by above aparently: sudo apt-get install tesseract-ocr-eng

#Rest framework: (From  http://www.django-rest-framework.org/ )

sudo pip install djangorestframework
sudo pip install markdown       # Markdown support for the browsable API.
sudo pip install django-filter  # Filtering support